apiVersion: apps/v1
kind: Deployment
metadata: 
  name: payments-service-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: payments-service
  template:
    metadata:
      labels:
        app: payments-service
    spec:
      containers:
        - name: payments-service
          image: us.gcr.io/ticketing-dev-504919/payments-service
          env:
            - name: NATS_CLIENT_ID
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: NATS_URL
              value: 'http://nats-srv:4222'
            - name: NATS_CLUSTER_ID
              value: 'ticketing'
            - name: JWT_KEY
              valueFrom:
                secretKeyRef:
                  name: jwt-secret
                  key: JWT_KEY
            - name: STRIPE_KEY
              valueFrom:
                secretKeyRef:
                  name: stripe-secret
                  key: STRIPE_KEY
            - name: COOKIE_SECURE
              value: 'false'
            - name: MONGO_URI
              value: 'mongodb://payments-mongo-srv:27017/payments'
---
apiVersion: v1
kind: Service
metadata:
  name: payments-service-srv
spec:
  selector:
    app: payments-service
  ports:
    - name: payments-service
      protocol: TCP
      port: 8000
      targetPort: 8000
